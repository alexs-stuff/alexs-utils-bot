import { Client, Interaction } from "discord.js";

module.exports = {
    name: "ping",
    description: "pings the bot",
    onCallback: async (client:any, interaction:any) => {
        //fuck you typescript for making me put class type
    }
}